# v0.94.3

## 修复

- **工作树模式下终端命令不再被误判越界**：会话绑定工作树后，命令的默认目录直接落在工作树里，执行命令不会再被「不在允许目录」挡下。
- **工作树模式下写文件与打补丁不再被误判越界**：在工作树里改文件、应用补丁能正常通过权限校验，不再提示目标不在允许范围。
- **进行中任务在磁贴与会话工具栏同步显示**：AI 创建或完成任务、调度器触发任务时，磁贴卡片与工具栏胶囊会即时刷新状态，不再需要切换会话才更新。

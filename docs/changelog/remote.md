# 变更日志

## 发布：v0.90.1

## 功能

- **能力商店**：技能来源新增 ClawHub，可浏览、搜索并安装 OpenClaw 社区的公开技能；条目详情里的来源地址现在一点即可在浏览器打开。
- **文件与链接操作**：在对话、归档等位置对文件链接或网页链接点右键会弹出操作菜单——文件可在侧边打开、在 VS Code 中打开、定位文件、另存为、复制路径；网页链接可在浏览器打开或复制链接。
- **看图更直接**：用本身支持读图的模型读取本地图片时，直接把原图交给模型理解，不再先经过一次文字转述。

## 修复

- 录音时切换到别的窗口再松开录音快捷键，录音仍会继续、往往录到上限才停，现在能正常停下。
- 配置窗口变窄时，能力商店、连接器、技能、远程渠道等页面的标题栏与分段切换控件不再被容器裁掉或外凸。

## 发布：v0.90.0

## 功能

- **能力商店**：配置页新增能力商店，可跨魔搭、Smithery、官方 MCP Registry 检索 MCP 与技能并一键安装，装进来的默认是关闭状态；需要密钥的 MCP 会在安装前要求填写。
- **技能管理**：技能此前只能查看和编辑，现在可以整体启用或关闭；列表区分为自定义技能与系统内置技能，自定义技能能改名称、描述和正文，也能删除，内置技能只读；技能附带的其他文件可以直接查看。
- **配置页改版**：技能、供应商、部门、人格、连接器、渠道六个模块，由下拉单选加长表单改为卡片概览，点进卡片看详情；部门与人格增加「自定义 / 系统预设」分类，各模块都支持搜索。
- 人格设定词和技能正文支持 Markdown 预览与编辑切换。

## 修复

- 在输入框用 Tab 打开快捷指令、或用 @ 打开提及面板时，弹层被会话工具条压住、选项看不全，现在正常盖在工具条之上（引入于 v0.89.0）。
- Codex 供应商的模型下拉里缺少 gpt-6-astra，现在会随后端清单自动出现（引入于 v0.84.0）。
- macOS 26 上让 AI 模拟组合键会让应用直接崩溃，现已不再触发（引入于 v0.9.9）。

## 发布：v0.89.0

## 功能

- **PAI CLI（Beta）**：在终端里也能用 PAI。不开窗口、不弹界面，给一句任务跑完一整轮（含多轮工具调用），最终回答直接打在终端上；桌面端配好的 skill 与 MCP 在 CLI 里同样可用。用法 `p-ai run "<任务>" --model <配置>`。
- **流式预览**：聊天窗输入框上方新增预览条。生成中，上面四行思维链按阅读节奏逐行推进，下面一行展示最新一段正文；不生成时显示最新一条回复的正文。它同时接过「回到最新」的入口——聊天窗右下角那个圆形「回到底部」按钮已移除。
- **操作区重做**：时间线按钮、预览条、工作区栏这三处原来各自漂浮的元素，收进输入框上方同一个操作区，分上下两排。贴底时显示下排工作区栏，往上翻时显示上排预览条与时间线按钮。
- 操作区下排的元素改为彼此独立：工作区、运行监控、自动推送各自按条件出现，放不下自动换行，点一次直接执行（原来是一条会伸缩的胶囊，点一次只是展开）。工具栏上的 @ 提及入口去掉，输入框自带的面板已经覆盖。
- 生成过程中向上翻看历史，视口不再被强行拽回底部；回到最下方或点预览条才恢复跟随。
- 发送消息后，你的消息停在视口顶部，回复从下方接上。
- 运行监控胶囊按运行类型分流：只跑委托或任务时打开对应页，只跑后台进程、或多种混合时打开主页卡片墙。
- 主页面板新增「Git 提交」卡，能直接看到最近的提交。
- 设置菜单左侧加上分组标题，部分分组可折叠；折叠时组里有更新提示会在标题上显示红点。

## 修复

- 展开「思考与工具」时思维链先撑开再收起，现在直接以折叠态出现。
- 切换会话后，Git 更改区可能显示成别的会话仓库。
- 切换会话后，正在输出的助理气泡会丢掉流式样式。
- 打开会话先闪出第一条消息、不落在最新一条。
- 左右分栏打开时，时间线浮层会飘出聊天列。
- 自主权限档位在界面上与「全权」混同、权限范围被说小，现在单独显示为「无限制」。

## 发布：v0.88.0

## 功能

- 右侧栏预览卡片墙重做，新增「历史委托」卡与「计划预览」卡；Git 卡会随 IDE、终端里的文件改动自动刷新。
- 会话列表新增精简视图模式，运行中的会话有更明显的状态提示。
- 新增「深度回忆」工具，可以从历史会话里找回某个话题的来龙去脉。

## 修复

- 上下文占用与词元账单一直显示不对，现在会跟随会话最新进展。
- 会话正在输出时无法打开上下文预览面板，现在随时可以打开。
- 从「批准计划执行」等非发送入口继续会话时，回复收不到、界面卡在等待，现已恢复。
- 调度日志里一轮工具循环的多轮被合并成一轮，现在按轮次分别展示。
- 设置页内容紧贴导航栏、联系人卡片贴边错位，现已恢复内边距。

## 发布：v0.87.0

## 功能

- 右侧栏改为工作区预览卡片墙：左上角下拉按钮变成预览入口，Git 更改、已打开文件、工作目录、新建追问、追问会话、后台终端、委托、任务、最近工具各成一张卡片，没有内容就不占位；右侧栏同时加宽以容纳两列。
- 部门与人格归属改为双向勾选：人格页新增「归属部门」，与部门页的勾选人格对称；保存时不再替你自动补齐部门成员，空缺只提示、不阻断。
- 人格回忆方式改为三档独立说明：「全功能 / 仅主动回忆 / 完全关闭」，说明随所选档位切换；选「完全关闭」时记忆不再写入、也不再召回。
- 行内代码与链接改为按主题混色：去掉行内代码的不透明白底，深浅主题下自动调制明暗，不再出现成片代码盖住文字。
- 新建会话页与追问空态的背景光斑改为跟主题取色的辉光：新建会话页从头像主色采样，追问空态收成一团主题色辉光放在按钮身后。

## 修复

- 切换人格后输入面板与首条消息仍沿用旧人格，要发出第一句才刷新，现在切换即时生效。
- macOS 上每次调用桌面操作都会弹出辅助功能授权请求，现在改为静默检查权限，缺失时给出可直接执行的开启指引。
- 从「更多」展开的全量人格卡片墙里选中人格后会立刻收起，连续比较要反复展开，现在选中后保持展开。
- 固定 macOS 发布签名，已授予的系统权限许可可以跨版本复用，升级后不必重新授权。

## 发布：v0.86.0

## 功能

- AI 操作桌面应用更可靠：支持等待窗口或元素出现、副屏窗口寻址、拖拽、剪贴板读写，后台点击改按窗口名和元素名定位，找不到时给出候选清单，脚本中途出错也会说明已执行到哪一步。
- 覆盖模式下打开文件后自动收起目录树，不用再手动切换才能看到内容。

## 修复

- 思考与执行字符数在流式回复时停在旧值，现在会持续增长到最新数字。
- 修复压缩重开对话后气泡永久停留在流式状态、与引导消息气泡并存的问题。
- 修复流式回复期间聊天区尾部留白被冻结、不再自适应的问题。
- 修复 Git 面板反复自动刷新停不下来的问题。
- 修复运行日志窗口不再自动跟随底部新日志的问题，滚动条改为常驻显示。
- 修复部门权限面板中 MCP 工具全部归入「其他」分组的问题。
- 修复用户消息在分享图片中重新折行的问题。

## 发布：v0.85.0

## 功能

- AI 修改文件不再等待审批：工作区权限为「审批」时，新建、修改、重命名、删除文件会直接执行，不再弹出智能评估与人工审批确认；每次改动在执行前仍会留下备份，可以照常撤回。
- AI 删除文件改为移入系统回收站（macOS 为废纸篓），不再永久删除，误删后可以从回收站找回。
- 终端命令审批要求 AI 说明用途：请求执行命令时必须填写这段命令要做什么，审批卡与审批弹窗会直接展示这段说明，方便你判断是否放行。
- 输入面板与提问卡统一为双层卡片：桌面与手机使用同一套布局，等待队列以「露头」方式提示，排队消息不再出现不显示的情况。
- 会话菜单的二级入口改为可折叠的多级点按展开。
- 回复文字的流式呈现更平滑：去掉尾部逐词淡入，改为按内容量自动调节速度。
- 时间轴预览卡字号与正文对齐并加宽，跳转时按距离区分平滑滚动。

## 修复

- 足迹墙「今天」视图在凌晨 0:00-4:00 会漏掉当天用量，现在凌晨时段的数据也会统计进来。
- 修复远程 IM 私聊中你发出的消息被误显示为助理消息、并出现空白头像的问题。
- 修复微信联系人发来的图片和文件收不到的问题，私聊发言人正确显示为本机用户。
- 工具执行被拒绝后仍显示为成功、并照常统计代码增删行数的问题，现在会明确标注已拒绝状态。
- 窗口切回或移动端恢复聊天时的问题：不再需要操作两次才恢复，进程被杀后切回不再无响应，后台正在进行的回复也不会被前台恢复打断。
- 队列出队后后续消息气泡刷不出的问题，队列状态不匹配时也会给出准确提示。
- 新会话的引导气泡有时不显示的问题。
- 便携版更新时程序被占用导致替换失败的问题：现在会等待程序退出后重试，并提供手动替换入口。

## 发布：v0.84.1

## 功能

- 优化概览展示。

## 发布：v0.84.0

## 功能

- 后台任务监控面板：AI 启动的后台命令可以在侧边栏实时查看运行状态、持续时长和输出进展。
- 会话工作面板的监控入口升级：委托、任务、后台进程同时运行时直接显示组合数量，单类运行保持原有详情样式；监控概览页集中查看所有进行中的事项。
- 任务状态变化现在实时同步到网页端与 VS Code 侧边栏，各端打开同一会话看到的运行进度保持一致；断网恢复或窗口切回前台后，进度会自动刷新为最新状态。
- 接入 OpenCode Go 端点时自动携带会话标识，修复请求被 OpenCode 以「缺少会话头」拒绝的问题。
- Code X 支持 GPT-6（gpt-6-astra）：可选模型清单新增该模型。

## 发布：v0.83.0

## 功能

- 桌面操作能力新增后台操作模式：助手无需切换窗口、不抢焦点，就能直接对指定后台窗口截图、点击（含双击）、填写文本、读取文本、滚动和发送按键——例如地址栏填完网址后直接回车导航，全程不打断你正在做的事。操作结果会报告实际生效的投递方式和操作后焦点落点，界面变化后重复旧操作会得到明确报错而不是错误动作。
- 桌面操作能力增强：助手执行屏幕操作时新增了「移动鼠标」动作，可以把光标移动到指定位置（例如悬停展示提示）而无需点击。

## 修复

- DeepSeek 通过 OpenAI Responses 协议接入时，选择「不思考」现在能真正关闭思维链；此前该选项对 Responses 协议不生效，模型仍会输出思考过程。

## 性能

- 打开含超长思维链或大代码块的消息后，滚动聊天不再周期性卡顿：隐藏弹窗中的原始消息 JSON 与代码全文预览改为打开时才渲染。
- 界面整体响应更流畅：工具评审详情、内置工具目录、远程 IM 日志、运行日志、提示词预览、更新说明、任务编辑、会话维护、批量归档等弹窗关闭后不再在后台保留重内容渲染。

## 发布：v0.82.6

## 修复

- 新建对话的目录下拉不再丢失候选：改为持久化最近使用与侧边栏全量分组两源合并的纯函数，路径大小写与斜杠已归一，每次打开结果一致，切换目录不再让已选目录消失或改名。
- 供应商模型能力同步改为防抖合并：切换供应商不再自动勾选或裁剪思维等级，仅置灰提示需手动重置，保存前会等待在途的模型信息解析完成，避免保存后按钮回弹。
- 文件管理器在目录未改动时不再频繁刷新：监听收敛为当前文件与已展开目录，目录签名已覆盖子项无需对每个可见文件单独轮询，根目录的 Git 内部变动不再计入检测，多窗口按会话隔离各自推送；未展开目录的文件变动不再触发刷新。
- 时间线浮层改为视口锚定：不再被消息区裁剪，监听窗口尺寸、滚动与可视视口实时重算可用区，时间线棋盘在各种窗口尺寸下保持可视。
- 切换目录时页面不再跳动：分支信息区改为常驻占位、不可用时仅置灰禁用；目录下拉选择时不再瞬移到右侧再消失。

## 重构

- 聊天与文件阅读器的虚拟列表统一迁移至 virtua，移除 tanstack 依赖，历史消息加载与文件内容滚动不再闪动，历史补丁与隐藏预量逻辑已清理。
- 设置页改为左右 flex 布局，去除虚化与多余边距，动效收敛；浅色主题字体与对比度、主色层级已重设，界面更清晰。

## 性能

- 文件管理器轮询量由可见行数降至展开目录数，700 毫秒指纹轮询在静止目录下不再触发重载，批量变更仅记录首条差异便于定位抖动源。

## 发布：v0.81.2

## 修复

- 流式回复的空档期不再让思考面板抢跑：调度状态文案区分为"请求中"与"思考中"，等待调度时只在上方显示一行请求状态，不再弹出空的思考/工具面板。
- 聊天右侧的阅读面板在窄窗口下打开目录树时，目录树会占满整个面板、文件内容区让位，不再出现半透明的遮罩浮层，浏览目录与阅读文件切换更干净。
